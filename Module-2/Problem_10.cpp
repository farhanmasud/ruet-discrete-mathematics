#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    cout.precision(17);
    double golden_ratio = (1.0 + sqrt(5.0)) / 2.0;

    cout << "The value of the golden ratio: " << golden_ratio;

    return 0;
}
